-module(lesson2_task01).
-export([last/1]).

last([X]) -> X;  % Якщо в списку лише один елемент, то він і є останнім.
last([_ | Tail]) -> last(Tail).  % Якщо елемент не останній, викликаємо функцію для решти списку.