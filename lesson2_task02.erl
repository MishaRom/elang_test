-module(lesson2_task02).
-export([but_last/1]).

but_last([X, Y]) -> [X, Y];  % Якщо список містить тільки два елементи, то повертаємо їх.
but_last([_ | Tail]) -> but_last(Tail).  % Відкидаємо перший елемент і викликаємо функцію для решти списку.